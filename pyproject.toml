[tool.poetry]
authors = ["Bas Brussee", "rako79"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Framework :: AsyncIO",
  "Intended Audience :: Developers",
  "Natural Language :: English",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: 3",
]
description = "Adaptive Cover rako Edition for Home Assistant"
documentation = "https://github.com/rako79/adaptive-cover"
homepage = "https://github.com/rako79/adaptive-cover"
license = "MIT"
maintainers = ["rako79"]
name = "adaptive_cover"
packages = []
readme = "README.md"
repository = "https://github.com/rako79/adaptive-cover"
version = "1.6.0"

[tool.poetry.dependencies]
homeassistant = ">=2026.7.0,<2027.0.0"
python = ">=3.14.2,<3.15"

[tool.poetry.group.dev.dependencies]
pre-commit = "4.0.1"
pre-commit-hooks = "5.0.0"
pylint = "3.3.1"
pytest-homeassistant-custom-component = "0.13.348"
ruff = "0.9.1"

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.poetry.urls]
"Bug Tracker" = "https://github.com/rako79/adaptive-cover/issues"
Changelog = "https://github.com/rako79/adaptive-cover/releases"

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core"]

[tool.ruff]
ignore = [
  "ANN101", # Self... explanatory
  "ANN401", # Opiniated warning on disallowing dynamically typed expressions
  "D203", # Conflicts with other rules
  "D213", # Conflicts with other rules
  "TID252", # Relative imports
  "RUF012", # Just broken

  # Formatter conflicts
  "COM812",
  "COM819",
  "D206",
  "E501",
  "ISC001",
  "Q000",
  "Q001",
  "Q002",
  "Q003",
  "W191",
]
select = ["ALL"]
src = ["custom_components/adaptive_cover"]

[tool.ruff.flake8-import-conventions.extend-aliases]
"homeassistant.helpers.area_registry" = "ar"
"homeassistant.helpers.config_validation" = "cv"
"homeassistant.helpers.device_registry" = "dr"
"homeassistant.helpers.entity_registry" = "er"
"homeassistant.helpers.issue_registry" = "ir"
voluptuous = "vol"

[tool.ruff.isort]
force-sort-within-sections = true
known-first-party = [
    "homeassistant",
]
combine-as-imports = true

[tool.pylint."MESSAGES CONTROL"]
# Reasons disabled:
# format - handled by ruff
# duplicate-code - unavoidable
# used-before-assignment - false positives with TYPE_CHECKING structures
disable = [
  "format",
  "duplicate-code",
  "used-before-assignment",
]
